services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teamcodebridge-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
